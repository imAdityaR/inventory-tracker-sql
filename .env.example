# Copy this file to ".env" and fill in your actual PostgreSQL credentials.
# Never commit your real .env file -- it's already excluded in .gitignore.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=inventory_tracker
DB_USER=postgres
DB_PASSWORD=your_password_here
